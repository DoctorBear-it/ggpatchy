% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scale_pattern.R
\name{scale_pattern_manual}
\alias{scale_pattern_manual}
\title{Map discrete variable to fill patterns}
\usage{
scale_pattern_manual(values, ...)
}
\arguments{
\item{values}{Named or unnamed character vector of pattern names. Built-in
base patterns: \code{"none"}, \code{"hatch"}, \code{"crosshatch"}, \code{"horizontal"},
\code{"vertical"}, \code{"dots"}, \code{"weave"}. Each base pattern (except \code{"none"})
also has \verb{_dense} and \verb{_sparse} variants (e.g. \code{"hatch_dense"},
\code{"crosshatch_sparse"}) that use a pre-set tighter or looser spacing.
Custom patterns registered via \code{\link[=register_pattern]{register_pattern()}} are also accepted.}

\item{...}{Passed to \code{\link[ggplot2:discrete_scale]{ggplot2::discrete_scale()}}.}
}
\description{
Maps the levels of a discrete variable to specific pattern names. When
\code{values} is named, names are matched to the variable's levels (so the
order of \code{values} does not need to match the level order). When unnamed,
values are matched positionally to levels.
}
\examples{
library(ggplot2)
ggplot(mpg, aes(class, fill = class, pattern = class)) +
  geom_bar_pattern() +
  scale_pattern_manual(values = c(
    "suv"        = "crosshatch",
    "compact"    = "hatch",
    "midsize"    = "dots",
    "minivan"    = "horizontal",
    "pickup"     = "vertical",
    "subcompact" = "weave",
    "2seater"    = "none"
  ))
}
