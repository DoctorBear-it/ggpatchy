% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geom_rect_pattern.R
\name{GeomRectPattern}
\alias{GeomRectPattern}
\alias{GeomTilePattern}
\alias{geom_rect_pattern}
\alias{geom_tile_pattern}
\title{Rectangle and tile charts with pattern overlays}
\usage{
geom_rect_pattern(
  mapping = NULL,
  data = NULL,
  stat = "identity",
  position = "identity",
  ...,
  na.rm = FALSE,
  show.legend = NA,
  inherit.aes = TRUE
)

geom_tile_pattern(
  mapping = NULL,
  data = NULL,
  stat = "identity",
  position = "identity",
  ...,
  width = NULL,
  height = NULL,
  na.rm = FALSE,
  show.legend = NA,
  inherit.aes = TRUE
)
}
\arguments{
\item{mapping}{Aesthetic mappings created by \code{\link[ggplot2:aes]{ggplot2::aes()}}.}

\item{data}{Data frame.}

\item{stat}{Statistical transformation. Default \code{"identity"}.}

\item{position}{Position adjustment. Default \code{"identity"}.}

\item{...}{Other arguments passed to the layer.}

\item{na.rm}{If \code{FALSE} (default), missing values are removed with a warning.}

\item{show.legend}{Logical. Should this layer be included in the legend?}

\item{inherit.aes}{If \code{FALSE}, overrides the default aesthetics.}

\item{width, height}{Tile width and height in data units. If \code{NULL}
(default), computed as the resolution of \code{x}/\code{y}.}
}
\value{
A ggplot2 layer.
}
\description{
Drop-in replacements for \code{\link[ggplot2:geom_rect]{ggplot2::geom_rect()}} and \code{\link[ggplot2:geom_tile]{ggplot2::geom_tile()}}
that add a \code{pattern} aesthetic. Patterns are drawn on top of a filled
rectangle and clipped to its bounding box.
}
\details{
\code{geom_rect_pattern()} requires explicit corner aesthetics (\code{xmin}, \code{xmax},
\code{ymin}, \code{ymax}). \code{geom_tile_pattern()} accepts center + size aesthetics
(\code{x}, \code{y} plus optional \code{width} and \code{height}; widths/heights default to the
resolution of the x/y values so tiles tessellate without gaps).
}
\section{Pattern aesthetics}{

\describe{
\item{\code{pattern}}{Character name of the pattern. One of \code{"none"},
\code{"hatch"}, \code{"crosshatch"}, \code{"horizontal"}, \code{"vertical"}, \code{"dots"},
\code{"weave"}, or a custom pattern registered with \code{\link[=register_pattern]{register_pattern()}}.
Each base pattern (except \code{"none"}) also has \verb{_dense} and \verb{_sparse}
variants (e.g. \code{"hatch_dense"}, \code{"dots_sparse"}) for pre-set tighter
or looser spacing.}
\item{\code{pattern_colour}}{Colour of pattern lines/dots. Default \code{"black"}.}
\item{\code{pattern_linewidth}}{Line width for line-based patterns. Default \code{1}.}
\item{\code{pattern_spacing}}{Spacing between pattern elements in millimetres.
Default \code{5}. Smaller values produce denser patterns; larger values produce
sparser patterns. Named density variants (e.g. \code{"hatch_dense"}) bake
in a pre-set spacing multiplier but still respect explicit
\code{pattern_spacing} values.}
\item{\code{pattern_angle}}{Angle in degrees for hatch patterns. Default \code{45}.}
\item{\code{pattern_size}}{Dot radius in millimetres for the \code{"dots"}
pattern. Default \code{0.5}.}
}
}

\examples{
library(ggplot2)

# Explicit corner coordinates
df <- data.frame(
  xmin = c(0, 1, 2), xmax = c(0.8, 1.8, 2.8),
  ymin = 0, ymax = c(1, 3, 2),
  pattern = c("hatch", "crosshatch", "dots")
)
ggplot(df, aes(xmin = xmin, xmax = xmax, ymin = ymin, ymax = ymax,
               fill = pattern, pattern = pattern)) +
  geom_rect_pattern() +
  scale_pattern_identity() +
  scale_fill_brewer(palette = "Pastel1") +
  theme_minimal()

# Heatmap-style tile chart
df2 <- expand.grid(x = 1:4, y = 1:4)
df2$pattern <- rep(c("hatch", "crosshatch", "dots", "none"), 4)
df2$fill_g  <- rep(letters[1:4], each = 4)
ggplot(df2, aes(x, y, fill = fill_g, pattern = pattern)) +
  geom_tile_pattern() +
  scale_pattern_identity() +
  scale_fill_brewer(palette = "Pastel2") +
  theme_minimal()
}
