% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scale_pattern.R
\name{scale_pattern_discrete}
\alias{scale_pattern_discrete}
\title{Automatically assign patterns to a discrete variable}
\usage{
scale_pattern_discrete(...)
}
\arguments{
\item{...}{Passed to \code{\link[ggplot2:discrete_scale]{ggplot2::discrete_scale()}}.}
}
\description{
Cycles through the built-in pattern set in a fixed order
(\code{hatch}, \code{crosshatch}, \code{dots}, \code{horizontal}, \code{vertical}, \code{weave}, \code{none}),
assigning each level the next pattern in the cycle.
}
\details{
Note: assignment is \strong{positional against factor levels}, not by name.
If your variable's values \emph{are} pattern names (e.g. \code{"hatch"}, \code{"dots"})
and you want each level rendered as its own name, use
\code{\link[=scale_pattern_identity]{scale_pattern_identity()}} instead.
}
\examples{
library(ggplot2)
df <- data.frame(g = letters[1:4], v = c(3, 5, 2, 4))
ggplot(df, aes(g, v, fill = g, pattern = g)) +
  geom_col_pattern() +
  scale_pattern_discrete()
}
