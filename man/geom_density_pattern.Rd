% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geom_density_pattern.R
\name{GeomDensityPattern}
\alias{GeomDensityPattern}
\alias{geom_density_pattern}
\title{Kernel density estimates with pattern overlays}
\usage{
geom_density_pattern(
  mapping = NULL,
  data = NULL,
  stat = "density",
  position = "identity",
  ...,
  na.rm = FALSE,
  orientation = NA,
  show.legend = NA,
  inherit.aes = TRUE
)
}
\arguments{
\item{mapping}{Aesthetic mappings created by \code{\link[ggplot2:aes]{ggplot2::aes()}}.}

\item{data}{Data frame.}

\item{stat}{Statistical transformation. Default \code{"density"}.}

\item{position}{Position adjustment. Default \code{"identity"}.}

\item{...}{Additional arguments passed to \code{\link[ggplot2:stat_density]{ggplot2::stat_density()}} (e.g.
\code{bw}, \code{adjust}, \code{kernel}, \code{n}).}

\item{na.rm}{If \code{FALSE} (default), missing values are removed with a warning.}

\item{orientation}{Orientation of the layer. Default \code{NA} (automatic).}

\item{show.legend}{Logical. Should this layer be included in the legend?}

\item{inherit.aes}{If \code{FALSE}, overrides the default aesthetics.}
}
\value{
A ggplot2 layer.
}
\description{
A drop-in replacement for \code{\link[ggplot2:geom_density]{ggplot2::geom_density()}} that adds a \code{pattern}
aesthetic. The pattern is clipped to the filled area under the density
curve using the same device-independent in-R geometry as
\code{\link[=geom_ribbon_pattern]{geom_ribbon_pattern()}}.
}
\section{Pattern aesthetics}{

In addition to all aesthetics accepted by \code{\link[ggplot2:geom_density]{ggplot2::geom_density()}}, this
geom accepts:
\describe{
\item{\code{pattern}}{Character name of the pattern. One of \code{"none"},
\code{"hatch"}, \code{"crosshatch"}, \code{"horizontal"}, \code{"vertical"}, \code{"dots"},
\code{"weave"}, or a custom pattern registered with \code{\link[=register_pattern]{register_pattern()}}.
Each base pattern (except \code{"none"}) also has \verb{_dense} and \verb{_sparse}
variants (e.g. \code{"hatch_dense"}, \code{"dots_sparse"}) for pre-set tighter
or looser spacing.}
\item{\code{pattern_colour}}{Colour of pattern lines/dots. Default \code{"black"}.}
\item{\code{pattern_linewidth}}{Line width for line-based patterns. Default \code{1}.}
\item{\code{pattern_spacing}}{Spacing relative to the density bounding box
(npc units). Default \code{0.08}.}
\item{\code{pattern_angle}}{Angle in degrees for hatch patterns. Default \code{45}.}
\item{\code{pattern_size}}{Dot diameter in mm for the \code{"dots"} pattern.
Default \code{0.4}.}
}
}

\section{Limitations}{

\code{orientation = "y"} (horizontal densities) is not supported for the pattern
overlay. The base density area renders correctly, but the pattern is
silently skipped. Use \code{coord_flip()} on a vertical density as a workaround.

\code{position = "stack"} is supported: stacked densities are filled from 0 to
the cumulative density, and the pattern follows the same filled region.
}

\examples{
library(ggplot2)

# Single density with hatch pattern
ggplot(faithful, aes(waiting)) +
  geom_density_pattern(pattern = "hatch", fill = "lightblue") +
  theme_minimal()

# Multiple groups with different patterns
ggplot(mpg, aes(hwy, fill = drv, pattern = drv)) +
  geom_density_pattern(alpha = 0.6) +
  scale_pattern_manual(values = c("4" = "hatch", "f" = "crosshatch",
                                  "r" = "dots")) +
  scale_fill_brewer(palette = "Pastel1") +
  theme_minimal()
}
