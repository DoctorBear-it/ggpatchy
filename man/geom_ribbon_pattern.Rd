% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geom_ribbon_pattern.R
\name{GeomRibbonPattern}
\alias{GeomRibbonPattern}
\alias{GeomAreaPattern}
\alias{geom_ribbon_pattern}
\alias{geom_area_pattern}
\title{Ribbon and area charts with pattern overlays}
\usage{
geom_ribbon_pattern(
  mapping = NULL,
  data = NULL,
  stat = "identity",
  position = "identity",
  ...,
  outline.type = "both",
  na.rm = FALSE,
  show.legend = NA,
  inherit.aes = TRUE
)

geom_area_pattern(
  mapping = NULL,
  data = NULL,
  stat = "identity",
  position = "stack",
  ...,
  outline.type = "upper",
  na.rm = FALSE,
  show.legend = NA,
  inherit.aes = TRUE
)
}
\arguments{
\item{mapping}{Aesthetic mappings created by \code{\link[ggplot2:aes]{ggplot2::aes()}}.}

\item{data}{Data frame.}

\item{stat}{Statistical transformation. Default \code{"identity"}.}

\item{position}{Position adjustment. Default \code{"identity"} for
\code{geom_ribbon_pattern()}, \code{"stack"} for \code{geom_area_pattern()}.}

\item{...}{Other arguments passed to the layer.}

\item{outline.type}{Which sides of the ribbon to draw. One of \code{"both"}
(default), \code{"upper"}, \code{"lower"}, or \code{"full"}.}

\item{na.rm}{If \code{FALSE} (default), missing values are removed with a warning.}

\item{show.legend}{Logical. Should this layer be included in the legend?}

\item{inherit.aes}{If \code{FALSE}, overrides the default aesthetics.}
}
\value{
A ggplot2 layer.
}
\description{
Drop-in replacements for \code{\link[ggplot2:geom_ribbon]{ggplot2::geom_ribbon()}} and \code{\link[ggplot2:geom_area]{ggplot2::geom_area()}}
that add a \code{pattern} aesthetic. Patterns are clipped to the ribbon polygon
using the same device-independent in-R geometry as \code{\link[=geom_polygon_pattern]{geom_polygon_pattern()}}.
}
\section{Pattern aesthetics}{

In addition to all aesthetics accepted by \code{\link[ggplot2:geom_ribbon]{ggplot2::geom_ribbon()}}, these
geoms accept:
\describe{
\item{\code{pattern}}{Character name of the pattern. One of \code{"none"},
\code{"hatch"}, \code{"crosshatch"}, \code{"horizontal"}, \code{"vertical"}, \code{"dots"},
\code{"weave"}, or a custom pattern registered with \code{\link[=register_pattern]{register_pattern()}}.
Each base pattern (except \code{"none"}) also has \verb{_dense} and \verb{_sparse}
variants (e.g. \code{"hatch_dense"}, \code{"dots_sparse"}) for pre-set tighter
or looser spacing.}
\item{\code{pattern_colour}}{Colour of pattern lines/dots. Default \code{"black"}.}
\item{\code{pattern_linewidth}}{Line width for line-based patterns. Default \code{1}.}
\item{\code{pattern_spacing}}{Spacing between pattern elements in millimetres.
Default \code{5}. Smaller values produce denser patterns; larger values produce
sparser patterns. Named density variants (e.g. \code{"hatch_dense"}) bake
in a pre-set spacing multiplier but still respect explicit
\code{pattern_spacing} values.}
\item{\code{pattern_angle}}{Angle in degrees for hatch patterns. Default \code{45}.}
\item{\code{pattern_size}}{Dot radius in millimetres for the \code{"dots"}
pattern. Default \code{0.5}.}
}
}

\section{Limitations}{

\code{orientation = "y"} (horizontal ribbons/areas) is not supported for the
pattern overlay. The base ribbon renders correctly, but the pattern is
silently skipped.

NA gaps in the ribbon data collapse into the surrounding polygon rather than
producing separate pattern segments.
}

\examples{
library(ggplot2)

# Confidence band around a sine wave
x <- seq(0, 2 * pi, length.out = 60)
df <- data.frame(x = x, ymin = sin(x) - 0.3, ymax = sin(x) + 0.3)
ggplot(df, aes(x, ymin = ymin, ymax = ymax)) +
  geom_ribbon_pattern(pattern = "hatch", fill = "lightblue") +
  theme_minimal()

# Stacked area chart with patterns
df2 <- data.frame(
  x       = c(1:5, 1:5),
  y       = c(1, 2, 3, 2, 1, 2, 1, 3, 2, 1),
  group   = rep(c("A", "B"), each = 5),
  pattern = rep(c("hatch", "crosshatch"), each = 5)
)
ggplot(df2, aes(x, y, fill = group, pattern = pattern)) +
  geom_area_pattern(position = "stack") +
  scale_pattern_identity(guide = "legend") +
  theme_minimal()
}
