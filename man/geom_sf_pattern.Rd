% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geom_sf_pattern.R
\name{GeomSfPattern}
\alias{GeomSfPattern}
\alias{geom_sf_pattern}
\title{SF polygon maps with pattern overlays}
\usage{
geom_sf_pattern(
  mapping = ggplot2::aes(),
  data = NULL,
  stat = "sf",
  position = "identity",
  ...,
  na.rm = FALSE,
  show.legend = NA,
  inherit.aes = TRUE
)
}
\arguments{
\item{mapping}{Aesthetic mappings created by \code{\link[ggplot2:aes]{ggplot2::aes()}}.}

\item{data}{An sf data frame or compatible object.}

\item{stat}{Statistical transformation. Default \code{"sf"}.}

\item{position}{Position adjustment. Default \code{"identity"}.}

\item{...}{Other arguments passed to the layer.}

\item{na.rm}{If \code{FALSE} (default), missing values are removed with a warning.}

\item{show.legend}{Logical or character. Should this layer be included in
the legends?}

\item{inherit.aes}{If \code{FALSE}, overrides the default aesthetics.}
}
\value{
A ggplot2 layer.
}
\description{
A drop-in replacement for \code{\link[ggplot2:geom_sf]{ggplot2::geom_sf()}} that adds a \code{pattern}
aesthetic. Patterns are clipped to the exact polygon boundary using the
same device-independent in-R geometry as \code{\link[=geom_polygon_pattern]{geom_polygon_pattern()}}.
Requires the \code{sf} package.
}
\note{
\strong{GEOMETRYCOLLECTION}: rows with GEOMETRYCOLLECTION geometry produce a
warning and no pattern overlay; the base geometry is still rendered.

\strong{Holes in polygons}: inner rings (holes / donuts) are silently ignored —
the pattern fills the entire outer boundary including hole areas. This is a
limitation of the in-R polygon clipping approach.
}
\section{Pattern aesthetics}{

In addition to all aesthetics accepted by \code{\link[ggplot2:geom_sf]{ggplot2::geom_sf()}}, this geom
accepts:
\describe{
\item{\code{pattern}}{Character name of the pattern. One of \code{"none"},
\code{"hatch"}, \code{"crosshatch"}, \code{"horizontal"}, \code{"vertical"}, \code{"dots"},
\code{"weave"}, or a custom pattern registered with \code{\link[=register_pattern]{register_pattern()}}.
Each base pattern (except \code{"none"}) also has \verb{_dense} and \verb{_sparse}
variants (e.g. \code{"hatch_dense"}, \code{"dots_sparse"}) for pre-set tighter
or looser spacing.}
\item{\code{pattern_colour}}{Colour of pattern lines/dots. Default \code{"black"}.}
\item{\code{pattern_linewidth}}{Line width for line-based patterns. Default \code{1}.}
\item{\code{pattern_spacing}}{Spacing relative to the polygon bounding box
(npc units). Default \code{0.08}.}
\item{\code{pattern_angle}}{Angle in degrees for hatch patterns. Default \code{45}.}
\item{\code{pattern_size}}{Dot size for the \code{"dots"} pattern. Default \code{0.35}.}
}
}

\section{Geometry type support}{

POLYGON and MULTIPOLYGON features receive pattern overlays. POINT and
LINESTRING features render correctly via the base \code{\link[ggplot2:geom_sf]{ggplot2::geom_sf()}} but
without pattern overlays (silently skipped). Mixed geometry types in the
same layer are supported.
}

\examples{
if (requireNamespace("sf", quietly = TRUE)) {
  library(ggplot2)
  library(sf)

  sq1 <- st_polygon(list(cbind(c(0,1,1,0,0), c(0,0,1,1,0))))
  sq2 <- st_polygon(list(cbind(c(1,2,2,1,1), c(0,0,1,1,0))))
  df  <- data.frame(
    region  = c("A", "B"),
    pattern = c("hatch", "dots"),
    geometry = st_sfc(sq1, sq2, crs = 4326)
  )
  sf::st_geometry(df) <- "geometry"

  ggplot(df) +
    geom_sf_pattern(aes(fill = region, pattern = pattern)) +
    scale_pattern_identity(guide = "legend") +
    scale_fill_brewer(palette = "Pastel1") +
    theme_minimal()
}
}
