% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geom_col_pattern.R
\name{GeomColPattern}
\alias{GeomColPattern}
\alias{geom_col_pattern}
\alias{geom_bar_pattern}
\title{Bar and column charts with pattern overlays}
\usage{
geom_col_pattern(
  mapping = NULL,
  data = NULL,
  position = "stack",
  ...,
  width = NULL,
  na.rm = FALSE,
  show.legend = NA,
  inherit.aes = TRUE
)

geom_bar_pattern(
  mapping = NULL,
  data = NULL,
  position = "stack",
  ...,
  width = NULL,
  na.rm = FALSE,
  show.legend = NA,
  inherit.aes = TRUE
)
}
\arguments{
\item{mapping}{Aesthetic mappings created by \code{\link[ggplot2:aes]{ggplot2::aes()}}.}

\item{data}{Data frame.}

\item{position}{Position adjustment. Default \code{"stack"}.}

\item{...}{Other arguments passed to the layer.}

\item{width}{Bar width, as a proportion of the bin width.}

\item{na.rm}{If \code{FALSE} (default), missing values are removed with a warning.}

\item{show.legend}{Logical. Should this layer be included in the legend?}

\item{inherit.aes}{If \code{FALSE}, overrides the default aesthetics.}
}
\value{
A ggplot2 layer.
}
\description{
These are drop-in replacements for \code{\link[ggplot2:geom_col]{ggplot2::geom_col()}} and
\code{\link[ggplot2:geom_bar]{ggplot2::geom_bar()}} that add support for a \code{pattern} aesthetic.
Map \code{pattern} to a discrete variable using \code{\link[=scale_pattern_manual]{scale_pattern_manual()}} or
\code{\link[=scale_pattern_discrete]{scale_pattern_discrete()}}.
}
\section{Pattern aesthetics}{

In addition to all aesthetics accepted by \code{\link[ggplot2:geom_col]{ggplot2::geom_col()}}, these
geoms accept:
\describe{
\item{\code{pattern}}{Character name of the pattern. One of \code{"none"},
\code{"hatch"}, \code{"crosshatch"}, \code{"horizontal"}, \code{"vertical"}, \code{"dots"},
\code{"weave"}, or a custom pattern registered with \code{\link[=register_pattern]{register_pattern()}}.}
\item{\code{pattern_colour}}{Colour of pattern lines/dots. Default \code{"black"}.}
\item{\code{pattern_linewidth}}{Line width for line-based patterns. Default \code{1}.}
\item{\code{pattern_spacing}}{Spacing between pattern elements as a fraction
of the bar width/height (npc units). Default \code{0.08}.}
\item{\code{pattern_angle}}{Angle in degrees for hatch patterns. Default \code{45}.}
\item{\code{pattern_size}}{Dot diameter in mm for the \code{"dots"} pattern.
Default \code{0.4}.}
}
}

\examples{
library(ggplot2)

# geom_bar_pattern uses stat="count" automatically
ggplot(mpg, aes(class, fill = class, pattern = class)) +
  geom_bar_pattern() +
  scale_pattern_discrete() +
  theme_minimal()

# geom_col_pattern needs x and y (pre-summarised data)
df <- data.frame(
  group = c("A", "B", "C"),
  value = c(3, 5, 4)
)
ggplot(df, aes(group, value, fill = group, pattern = group)) +
  geom_col_pattern() +
  scale_pattern_manual(values = c(A = "hatch", B = "dots", C = "crosshatch")) +
  scale_fill_brewer(palette = "Pastel1")
}
